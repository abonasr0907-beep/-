الدليل التقني الشامل — مكتب آفاق الإنجاز العقاري
الإصدار 2.0 | مرتب حسب الأولوية والتنفيذية
الجزء الأول: الإصلاحات العاجلة (8 مشاكل حالية)
1.  تعديل نظام "عرض عقارك"
الحالة: النموذج الحالي لا يحتوي على خريطة لتحديد الموقع.
الحل: أضف iframe لخرائط Google مع إمكانية وضع دبوس:
التوجيه: بدلاً من إدخال يدوي، اجعل النموذج يولّد JSON يُرسل للبوت عبر رابط WhatsApp مُنسّق:
function submitProperty() {
const data = {
name: document.getElementById('ownerName').value,
phone: document.getElementById('ownerPhone').value,
type: document.getElementById('propType').value,
area: document.getElementById('propArea').value,
price: document.getElementById('propPrice').value,
location: document.getElementById('propLocation').value
};
const msg = `📤 عرض عقار جديد\n👤 الاسم: ${data.name}\n📱 الجوال: ${data.phone}\n🏠 النوع: ${data.type}\n📐 المساحة: ${data.area}\n💰 السعر: ${data.price}\n📍 الموقع: ${data.location}`;
location.href = `https://wa.me/966545888931?text=${encodeURIComponent(msg)}`;
}
----
2.  إشعارات الاستبيان (الموافقة/الرفض)
الحالة: لا يوجد تأكيد بعد ملء الاستبيان.
الحل: أضف Toast Notification + إرسال تلقائي للبوت:
// في نهاية دالة إرسال الاستبيان
function submitSurvey() {
// ... جمع البيانات ...
// إشعار فوري للمستخدم
showToast("✅ تم استلام طلبك، سيصلك الرد خلال 24 ساعة");
// إرسال نسخة للبوت عبر WhatsApp API (Web)
const adminMsg = `📝 استبيان جديد\nالاسم: ${name}\nالرقم: ${phone}\nالنوع: ${interest}`;
fetch(`https://api.callmebot.com/whatsapp.php?phone=966545888931&text=${encodeURIComponent(adminMsg)}&apikey=YOUR_KEY`)
.catch(() => {}); // صامت في حالة الخطأ
// حفظ في LocalStorage للمتابعة
localStorage.setItem('afaq_pending_survey', JSON.stringify({date: Date.now(), status: 'pending'}));
}
----
3.  إصلاح القائمة الجانبية (الـ 3 خطوط)
الحالة: لا تظهر بالشكل الزجاجي.
الحل: استبدل الزر الحالي بهذا:
----
4.  إضافة قسم "لماذا آفاق الإنجاز" في الرئيسية
الموقع: بعد الـ Hero Section مباشرة.
الكود:
----
5.  تغيير خلفية الشعار (الـ Watermark)
الحالة: الشعار يظهر بشكل باهت.
الحل: استبدل body::before بهذا التدرج المعماري:
body::before {
content: "";
position: fixed; inset: 0; z-index: 0;
background:
radial-gradient(ellipse at 20% 50%, rgba(201, 162, 79, 0.08) 0%, transparent 50%),
radial-gradient(ellipse at 80% 20%, rgba(10, 58, 92, 0.4) 0%, transparent 50%),
radial-gradient(ellipse at 50% 100%, rgba(28, 90, 74, 0.15) 0%, transparent 50%),
linear-gradient(180deg, rgba(8, 20, 34, 0.3) 0%, rgba(8, 20, 34, 0.95) 100%),
url('../images/hero.jpg') center/cover no-repeat fixed;
pointer-events: none;
}
/* إخفاء الـ Watermark القديم أو تقليله */
.wm {
opacity: 0.03; /* تقليل الشفافية */
filter: blur(2px); /* تمويه خفيف */
}
----
6.  نظام حجز المعاينات المحسّن
الحالة: يوجه مباشرة لواتساب.
الحل: نموذج حجز + تسجيل في التقويم + إشعار للبوت:
<input type="text" id="bkName" placeholder="الاسم الكامل" style="width:100%;padding:14px 16px;margin-bottom:12px;border-radius:12px;border:1px solid var(--line);background:var(--glass2);color:#fff;font-family:inherit;">
<input type="tel" id="bkPhone" placeholder="رقم الجوال" style="width:100%;padding:14px 16px;margin-bottom:12px;border-radius:12px;border:1px solid var(--line);background:var(--glass2);color:#fff;font-family:inherit;">

<select id="bkDate" style="width:100%;padding:14px 16px;margin-bottom:12px;border-radius:12px;border:1px solid var(--line);background:var(--glass2);color:#fff;font-family:inherit;">
  <option value="">اختر اليوم</option>
  <option value="غداً">غداً</option>
  <option value="بعد غد">بعد غد</option>
  <option value="السبت">السبت القادم</option>
  <option value="الأحد">الأحد القادم</option>
</select>

<select id="bkTime" style="width:100%;padding:14px 16px;margin-bottom:20px;border-radius:12px;border:1px solid var(--line);background:var(--glass2);color:#fff;font-family:inherit;">
  <option value="">اختر الفترة</option>
  <option value="الصباح (٨ ص - ١٢ م)">الصباح (٨ ص - ١٢ م)</option>
  <option value="المساء (٤ م - ٨ م)">المساء (٤ م - ٨ م)</option>
</select>

<button onclick="confirmBooking()" style="width:100%;padding:16px;border-radius:14px;border:0;background:linear-gradient(135deg,var(--gold),var(--gold2));color:var(--night);font-family:inherit;font-size:16px;font-weight:800;cursor:pointer;">
  ✅ تأكيد الحجز
</button>
<button onclick="document.getElementById('bookingModal').style.display='none'" style="width:100%;margin-top:10px;padding:12px;border-radius:14px;border:1px solid var(--line);background:transparent;color:rgba(255,255,255,.6);font-family:inherit;font-size:14px;cursor:pointer;">
  إلغاء
</button>

----
7.  ترتيب روابط البوت (Short URLs)
الحالة: الروابط طويلة وغير منظمة.
الحل: استخدم slug من الـ JSON + إعادة توجيه:
// في cards.js — عند إنشاء رابط المشاركة
function getShareUrl(offer) {
const slug = offer.t
.replace(/[^\w\s]/gi, '')
.replace(/\s+/g, '-')
.substring(0, 30);
return `https://abonasr0907-beep.github.io/-/?offer=${offer._id}&s=${slug}`;
}
// مثال على الناتج:
// https://abonasr0907-beep.github.io/-/?offer=AFQ-001&s=mzrAA-nmwzGIA
----
8.  تعديل أرقام التواصل (التناوب)
الأرقام: 0544699933 (الأكثر حرارة) | 0545888931 (الطوارئ)
التعديل في index.html و cards.js:
const PHONE_PRIMARY = '0544699933';
const PHONE_WHATSAPP = '966545888931'; // واتساب ثابت
const PHONE_EMERGENCY = '0544699933'; // تناوب حسب الوقت
function getActivePhone() {
const hour = new Date().getHours();
// تناوب: 8 صباحاً - 8 مساءً = الرئيسي، باقي الوقت = الطوارئ
return (hour >= 8 && hour <= 20) ? '0544699933' : '0545888931';
}
// في أزرار الاتصال
function callPrimary() {
location.href = `tel:${getActivePhone()}`;
}
----
الجزء الثاني: الاقتراحات الـ20 مرتبة حسب الأولوية
المجموعة أ: Quick Wins (أسبوع 1-2) — لا تحتاج Backend
الاقتراح	التأثير	صعوبة	الكود
1	نظام "قارن بين العروض"	عالي	سهل	موجود جزئياً — تفعيل cmpSet
2	مؤشر "الحرارة" FOMO Badge	عالي	سهل	تعديل card()
3	شارة "عقار موثّق" Verified	عالي	سهل	إضافة حقل verified في JSON
4	نسخة PWA	متوسط	سهل	manifest.json + Service Worker
5	إشعارات دفع OneSignal	عالي	متوسط	كود جاهز أدناه
6	البحث المحفوظ والتنبيهات	متوسط	سهل	LocalStorage
7	بوصلة الموقع الذكية	متوسط	سهل	Geolocation API
8	"كل عقار حكاية" Storytelling	عالي	سهل	تعديل نصوص JSON فقط
المجموعة ب: متوسطة (أسبوع 3-4)
الاقتراح	التأثير	صعوبة
9	متابعة تلقائية واتساب	عالي	متوسطة
10	تكامل Google Calendar	متوسط	متوسطة
11	خرائط حي تفاعلية	عالي	متوسطة
12	تقرير السوق الأسبوعي	متوسط	سهل
13	تقييمات عملاء (فيديو)	عالي	صعب
14	نظام النقاط والمكافآت	متوسط	متوسطة
المجموعة ج: متقدمة (تتطلب Backend/مدفوعة)
الاقتراح	التأثير	التكلفة
15	وكيل صوتي ذكي	عالي	ElevenLabs / Google Cloud
16	تحليل المشاعر Sentiment	متوسط	OpenAI API
17	توجيه ذكي للعملاء	متوسط	Zoho / CRM
18	جولات 3D تفاعلية	عالي	Matterport
19	فيديو سينمائي POV	عالي	إنتاج فيديو
20	نظام "أجرِب قبل الشراء" Virtual Staging	متوسط	AI API
الجزء الثالث: أكواد جاهزة للتنفيذ
A. PWA — manifest.json
{
"name": "آفاق الإنجاز العقاري",
"short_name": "آفاق الإنجاز",
"description": "مزارع واستراحات وأراضي في الخرج والرياض - رخصة فال 1100004208",
"start_url": "/",
"display": "standalone",
"background_color": "#081420",
"theme_color": "#0A3A5C",
"orientation": "portrait",
"icons": [
{"src": "images/logo-72.jpg", "sizes": "72x72", "type": "image/jpeg"},
{"src": "images/logo-192.jpg", "sizes": "192x192", "type": "image/jpeg"},
{"src": "images/logo-512.jpg", "sizes": "512x512", "type": "image/jpeg"}
],
"shortcuts": [
{
"name": "عروضنا العقارية",
"short_name": "العروض",
"description": "تصفح أحدث العروض",
"url": "/categories.html",
"icons": [{"src": "images/icon-offers.png", "sizes": "96x96"}]
},
{
"name": "تواصل معنا",
"short_name": "التواصل",
"description": "احجز معاينة مجانية",
"url": "/contact.html",
"icons": [{"src": "images/icon-contact.png", "sizes": "96x96"}]
}
]
}
أضف في <head>:
B. Service Worker — sw.js
const CACHE_NAME = 'afaq-v1';
const STATIC_ASSETS = [
'/',
'/index.html',
'/css/ui-qwen.css',
'/js/cards.js',
'/images/logo.jpg',
'/images/hero.jpg'
];
self.addEventListener('install', (e) => {
e.waitUntil(
caches.open(CACHE_NAME).then((cache) => cache.addAll(STATIC_ASSETS))
);
self.skipWaiting();
});
self.addEventListener('fetch', (e) => {
e.respondWith(
caches.match(e.request).then((response) => {
return response || fetch(e.request).catch(() => {
// fallback للصفحة الرئيسية
if (e.request.mode === 'navigate') {
return caches.match('/index.html');
}
});
})
);
});
تسجيله في index.html قبل </body>:
C. OneSignal Push Notifications
زر الاشتراك المخصص:
<button onclick="subscribePush()" id="pushBtn" style="background:var(--glass);border:1.5px solid var(--gold);border-radius:99px;padding:12px 24px;color:var(--gold2);font-family:inherit;font-weight:800;cursor:pointer;">
🔔 فعّل الإشعارات
</button>
D. شارة "عقار موثّق" + FOMO Badge
// في دالة card() — استبدل الـ badge الحالي
const verifiedBadge = o.verified ?
`<span class="bdg" style="background:linear-gradient(135deg,#1c7a3d,#2d9d5a);color:#fff;border-color:#1c7a3d;">✓ موثّق آفاق</span>` :
`<span class="bdg">جديد</span>`;
const fomoBadge = o.views > 10 ?
`<span class="bdg" style="background:rgba(220,38,38,.8);color:#fff;border-color:#dc2626;right:auto;left:12px;top:44px;">🔥 ${o.views} مشاهدة</span>` : '';
// في الـ HTML داخل card():
d.innerHTML = `<div class="gal2">...${verifiedBadge}${fomoBadge}...`;
E. نظام النقاط (Gamification)
const POINTS = {
VISIT: 5,
OPEN_BOT: 10,
BOOK_TOUR: 50,
SHARE_OFFER: 20,
INSTALL_PWA: 100,
SUBSCRIBE_PUSH: 30
};
function addPoints(action) {
let total = parseInt(localStorage.getItem('afaq_points') || '0');
total += POINTS[action] || 0;
localStorage.setItem('afaq_points', total);
updateLevel(total);
return total;
}
function updateLevel(points) {
let level = 'زائر';
if (points >= 100) level = 'مستكشف';
if (points >= 300) level = 'مستثمر';
if (points >= 600) level = 'شريك';
localStorage.setItem('afaq_level', level);
}
// استدعِها عند الأحداث:
// addPoints('VISIT');
// addPoints('BOOK_TOUR');
F. Google Analytics 4 + Microsoft Clarity
G. خريطة حي تفاعلية (Leaflet.js — مثل suwar.sa)
H. SEO — Sitemap.xml (أنشئه في الجذر)
I. robots.txt (أنشئه في الجذر)
User-agent: *
Allow: /
Sitemap: https://abonasr0907-beep.github.io/-/sitemap.xml
Disallow: /offers-data/
Disallow: /js/modules/
J. JSON-LD ديناميكي لكل عرض
// أضف هذه الدالة في cards.js
function injectOfferSchema(offer) {
const schema = {
"@context": "https://schema.org",
"@type": "RealEstateListing",
"name": offer.t,
"description": `${offer.k} في ${offer.l} بمساحة ${offer.a} م²`,
"url": `https://abonasr0907-beep.github.io/-/?offer=${offer._id}`,
"image": offer._images?.[0] || "https://abonasr0907-beep.github.io/-/images/logo.jpg",
"offers": {
"@type": "Offer",
"price": offer.p.toString(),
"priceCurrency": "SAR",
"availability": "https://schema.org/InStock"
},
"address": {
"@type": "PostalAddress",
"addressLocality": offer.r,
"addressRegion": "منطقة الرياض",
"addressCountry": "SA"
},
"businessFunction": "http://purl.org/goodrelations/v1#Sell"
};
let script = document.getElementById('dynamic-schema');
if (!script) {
script = document.createElement('script');
script.id = 'dynamic-schema';
script.type = 'application/ld+json';
document.head.appendChild(script);
}
script.textContent = JSON.stringify(schema);
}
// استدعِها داخل openSheet()
----
الجزء الرابع: تعليمات Joules — Checklist التنفيذ
المرحلة 1: الإصلاحات العاجلة (قبل أي شيء)
•  [ ] 1. إضافة manifest.json + sw.js + تسجيل PWA
•  [ ] 2. إنشاء sitemap.xml و robots.txt
•  [ ] 3. إضافة قسم "لماذا آفاق الإنجاز" في index.html
•  [ ] 4. تعديل خلفية body::before (التدرج المعماري الجديد)
•  [ ] 5. إصلاح زر القائمة الجانبية (الـ 3 خطوط الزجاجية)
•  [ ] 6. تعديل نظام حجز المعاينات (النموذج + التقويم + الواتساب)
•  [ ] 7. إضافة خريطة Google في نموذج "عرض عقارك"
•  [ ] 8. تعديل أرقام التواصل للتناوب (8ص-8م / الطوارئ)
•  [ ] 9. تفعيل إشعارات الاستبيان (Toast + WhatsApp للبوت)
المرحلة 2: Quick Wins (SEO + UX)
•  [ ] 10. إضافة GA4 + Microsoft Clarity
•  [ ] 11. تفعيل OneSignal Push Notifications
•  [ ] 12. إضافة شارة "موثّق" + FOMO Badge على البطاقات
•  [ ] 13. تفعيل نظام المقارنة (cmpSet موجود مسبقاً)
•  [ ] 14. إضافة JSON-LD ديناميكي لكل عرض
•  [ ] 15. تعديل نصوص العروض لنمط "كل عقار حكاية"
المرحلة 3: تحسينات متوسطة
•  [ ] 16. نظام النقاط والمكافآت (LocalStorage)
•  [ ] 17. البحث المحفوظ والتنبيهات
•  [ ] 18. خرائط حي تفاعلية (Leaflet.js)
•  [ ] 19. تقرير السوق الأسبوعي (قسم ثابت يُحدّث يدوياً)
المرحلة 4: متقدمة (لاحقاً)
•  [ ] 20. تكامل Google Calendar الكامل
•  [ ] 21. متابعة تلقائية واتساب
•  [ ] 22. تقييمات عملاء بالفيديو
•  [ ] 23. وكيل صوتي / تحليل مشاعر (API مدفوع)
ملاحظات تنفيذية مهمة
1.  لا تلمس: offers-data/* و bot/data/* — هذه تُدار عبر بوت التلجرام
2.  GitHub Pages يدعم HTTPS تلقائياً (مطلوب للـ PWA والـ Push)
3.  OneSignal مجاني حتى 10,000 مشترك
4.  Microsoft Clarity مجاني تماماً
5.  استبدل جميع YOUR_... و G-XXXXXXXXXX بالقيم الفعلية
6.  الأرقام الحقيقية: 0544699933 (مكالمات) | 0545888931 (واتساب) | afaqalqary@gmail.com
----
