# دليل النشر — بوت آفاق الإنجاز العقاري

## الحالة الحالية

| المكون | الحالة | الرابط |
|--------|--------|--------|
| الموقع العام | ✅ يعمل | https://abonasr0907-beep.github.io/-/ |
| كود البوت | ✅ مُصلح ومُدمج | في المستودع (فرع main) |
| مزامنة GitHub | ✅ جاهزة | `bot/github_sync.py` |
| **استضافة البوت** | ❌ **غير مُفعّلة** | تحتاج نشر على منصة سحابية |

## لماذا توقف البوت؟

البوت يستخدم `run_polling()` أو `run_webhook()` — وكلاهما يحتاج **عملية تعمل باستمرار** على خادم.
عندما لا يكون البوت منشوراً على أي خادم سحابي، فإنه **لا يعمل** ببساطة.

`app.koyeb.com` لا يعمل بسبب مشاكل في الوصول من بعض المناطق. بدلاً منه، نستخدم **Render.com** (مجاني ويعمل في كل مكان).

## الحل: Render.com (مجاني + webhook)

البوت الآن يدعم **وضع webhook** — وهذا يعني:
- ✅ يعمل على **الخطة المجانية** (Web Service free tier)
- ✅ **يستيقظ تلقائياً** عند وصول رسالة من تيليجرام
- ✅ لا يحتاج عملية تعمل 24/7 (يوفّر الموارد)
- ✅ SSL تلقائي (مطلوب لـ webhook)

---

## خطوات النشر (مرة واحدة فقط)

### الخطوة 1: إنشاء حساب على Render
1. اذهب إلى: **https://dashboard.render.com/register**
2. سجّل بحساب GitHub (نفس حسابك `abonasr0907-beep`)

### الخطوة 2: إنشاء Web Service
1. في لوحة تحكم Render، اضغط **New +** → **Web Service**
2. اختر **Build and deploy from a Git repository**
3. اختر مستودعك: `abonasr0907-beep/-`
4. املأ الإعدادات:
   - **Name**: `afaq-bot`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r bot/requirements.txt`
   - **Start Command**: `cd bot && python3 bot.py`
   - **Plan**: `Free`
5. اضغط **Advanced** وأضف متغيرات البيئة (انظر الخطوة 3)
6. اضغط **Create Web Service**

### الخطوة 3: إضافة متغيرات البيئة
في صفحة الخدمة، اذهب إلى **Environment** وأضف:

#### متغير 1: `WEBHOOK_URL`
- القيمة: رابط خدمتك على Render (يظهر بعد النشر الأول)
- مثال: `https://afaq-bot-xxxx.onrender.com`
- **مهم**: لا تضع `/` في النهاية

#### متغير 2: `GITHUB_TOKEN`
- القيمة: توكن GitHub الشخصي (لرفع الوسائط للموقع)
- طريقة الحصول عليه:
  1. اذهب إلى: https://github.com/settings/tokens
  2. اضغط **Generate new token (classic)**
  3. ضع اسماً (مثل `afaq-bot-token`)
  4. اختر صلاحيات: **repo** (كاملة)
  5. اضغط **Generate token**
  6. انسخ التوكن وضعه هنا

### الخطوة 4: النشر الأول
1. اضغط **Manual Deploy** → **Deploy latest commit**
2. انتظر 2-3 دقائق حتى يكتمل البناء
3. ستحصل على رابط مثل: `https://afaq-bot-xxxx.onrender.com`

### الخطوة 5: تفعيل الـ webhook
1. انسخ رابط خدمتك من Render
2. اذهب إلى **Environment** على Render
3. ضع الرابط في `WEBHOOK_URL` (بدون `/` في النهاية)
4. احفظ → سيتم إعادة النشر تلقائياً
5. عند بدء التشغيل، البوت سيسجّل الـ webhook مع تيليجرام تلقائياً

### الخطوة 6: التحقق
1. افتح البوت على تيليجرام: @tlastlastlasbot
2. أرسل `/start`
3. يجب أن يرد البوت خلال ثوانٍ

---

## ملاحظات مهمة

### الخطة المجانية على Render
- الخدمة **تنام** بعد 15 دقيقة بدون نشاط
- **تستيقظ تلقائياً** عند وصول رسالة (قد تستغرق 30-50 ثانية للرسالة الأولى)
- بعد الاستيقاظ، الردود تكون فورية
- هذا مقبول تماماً لاستخدام إداري (أنت فقط من يستخدم البوت)

### رفع الوسائط للموقع
- عند رفع صورة عبر البوت، تُرفع تلقائياً إلى GitHub → الموقع يتحدث تلقائياً
- يتطلب `GITHUB_TOKEN` مُضبوط
- بدون التوكن، البوت يعمل لكن الصور لا تظهر على الموقع العام

### التشغيل المحلي (بديل)
إذا أردت تشغيل البوت على جهازك بدلاً من السحابة:
```bash
cd bot
pip install -r requirements.txt
python3 bot.py
```
(بدون `WEBHOOK_URL`، سيعمل في وضع polling — لا يحتاج إنترنت دائم لكن يجب أن يبقى الجهاز مفتوحاً)

---

## حل المشاكل

| المشكلة | الحل |
|---------|------|
| البوت لا يرد | تأكد أن `WEBHOOK_URL` مضبوط correctly |
| الصور لا تظهر على الموقع | تأكد أن `GITHUB_TOKEN` مضبوط بصلاحيات repo |
| Render يعرض خطأ في البناء | تأكد أن `requirements.txt` في مجلد `bot/` |
| البوت يستجيب ببطء | طبيعي على الخطة المجانية — أول رسالة تستغرق ~30 ثانية |

---

## البدائل الأخرى (إذا لم يعمل Render)

1. **Railway** (https://railway.app) — رصيد مجاني $5/شهر، لا ينام
2. **Fly.io** (https://fly.io) — 3 VMs مجانية، يحتاج CLI
3. **تشغيل محلي** — على جهازك الشخصي (يجب أن يبقى مفتوحاً)
