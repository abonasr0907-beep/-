# Phase 2 — Data Manifest

تُقرأ هذه الأعداد مباشرة من `offers-data/offers.json` (الفرع `feat/bot-manager-publish-lifecycle`)
عند بداية المرحلة الثانية. الهدف: قاعدة مرجعية للتأكد من عدم فقدان أي بيانات بعد التعديلات.

## الأعداد الأساسية

| المؤشر | القيمة |
|--------|--------|
| إجمالي العقارات | 25 |
| العقارات المنشورة (status=published أو بدون حقل status = legacy) | 25 |
| العقارات المعلّقة (status=pending) | 0 |
| عدد الصور الإجمالي | 50 |
| عقارات بدون صور | 1 |
| عقارات بدون category | 0 |
| عقارات فيها حقل status فعليًا | 0 (كلها legacy تُعامَل كـ published) |
| عقارات فيها external_id | 0 (تُنشأ عبر backfill وقت التشغيل) |

## توزيع الأقسام

| القسم | العدد |
|-------|-------|
| مزرعة | 8 |
| استراحة | 13 |
| أرض سكنية | 4 |
| عامة (بدون category) | 0 |

## ملاحظات

- جميع العقارات الحالية هي legacy (تُعامَل كـ published للتوافق الخلفي) كما هو موثّق في HANDOFF_1 القسم 11.
- `external_id` و `status` يُضافان للعقارات الجديدة فقط وقت التشغيل عبر listing_lifecycle backfill؛ العقارات القديمة تبقى كما هي.
- هذه الأعداد هي **baseline** — يجب أن لا تقل بعد أي تعديل في المرحلة الثانية.
- التحقق من عدم فقدان البيانات جزء من اختبارات القسم 5.
